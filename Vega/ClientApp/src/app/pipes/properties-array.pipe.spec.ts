import { PropertiesArrayPipe } from './properties-array.pipe';

describe('PropertiesArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new PropertiesArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
